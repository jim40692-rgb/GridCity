// ContentView.swift is no longer used.
// The app entry point is MainMenuView.
// This file is kept to avoid Xcode project file issues.

import SwiftUI

struct ContentView: View {
    var body: some View {
        MainMenuView()
    }
}
